

Функция ПроверкаДаты() Экспорт
	ДатаЗаписиПервогоЗапуска = Дата(Год(ТекущаяДата()), Месяц(ТекущаяДата()), День(ТекущаяДата()));
	ДатаВыводаФормы = Константы.ДатаВывода.Получить();
	Если ДатаЗаписиПервогоЗапуска <> ДатаВыводаФормы Тогда
			Константы.ДатаВывода.Установить(ДатаЗаписиПервогоЗапуска);
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

